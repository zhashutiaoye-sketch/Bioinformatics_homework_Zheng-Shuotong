# Week 5 Homework 1 - AI use and independent verification log

Student: 郑烁曈 (SUAT24000155)  |  File: `week5_deseq2_analysis.R`  |  Date: 2026-09-22

## 1. AI prompt (preserved)

> "Here is the Week 5 starter script `Week5_Homework_Starter.R` (DESeq2 bulk RNA-seq) and the
> assignment sheet `Week5_Homework_1_Instructions.md`. Turn the starter into a runnable script
> that follows required workflow steps 1-12: validate the inputs, build `~ batch + condition`,
> pre-filter with `rowSums(counts >= 10) >= 3`, inspect `resultsNames(dds)` instead of assuming
> the coefficient, shrink with apeglm, export the full shrunken table, draw one PCA and one
> volcano plot, save the fitted object and `sessionInfo()`, and leave the interpretation to me.
> Answer the two TODO comments in the starter and do not change the provided input files.
> Use only packages I have installed."

## 2. What the AI generated, and what was run locally

| Item | Status |
|---|---|
| `week5_deseq2_analysis.R` (drafted by AI from the starter) | run locally with `Rscript`, exit code 0 |
| Console transcript | `week5_run_console.txt` (complete, unedited) |
| Analysis objects | `week5_deseq2_results.csv`, `week5_deseq2_object.rds`, `session_info.txt` |
| Figures | `week5_pca.png`, `week5_de_plot.png` (both inspected visually before submission) |
| Interpretation (`week5_interpretation.md`) | drafted by AI, numbers cross-checked against the console output |

Reproduce with:

```bash
R_LIBS_USER="D:/R/R-userlib" Rscript week5_deseq2_analysis.R
```

## 3. Checks performed on the AI output (not taken on trust)

| # | Check | Result |
|---|---|---|
| 1 | Sample identity: `identical(colnames(counts), rownames(coldata))` | TRUE (this is an assertion in the script, not a comment) |
| 2 | Counts are non-negative integers; no duplicate sample IDs or gene IDs; no NA/Inf | TRUE |
| 3 | `control` is the reference level (`levels(coldata$condition)[1]`) | "control" |
| 4 | Design matrix is full rank for `~ batch + condition` | rank 4 of 4 columns |
| 5 | Coefficient name read from `resultsNames(dds)`, never assumed | `condition_treated_vs_control` confirmed; wrong name would make the script `stop()` |
| 6 | Direction of the effect (positive log2FC = up in treated) | consistent with the contrast `c("condition", "treated", "control")` |
| 7 | Package functions/arguments reviewed (`DESeqDataSetFromMatrix`, `DESeq`, `results`, `lfcShrink(type="apeglm")`, `vst`, `plotPCA`) | matches the current Bioconductor signatures for DESeq2 1.46.0 / apeglm 1.28.0 |
| 8 | Significant-gene count recomputed by hand from the exported table (padj < 0.05 and \|log2FC\| >= 1) | 60 (36 up / 24 down), same as the console |
| 9 | Estimates compared with the instructor truth key (989 genes) | Pearson r = 0.88, mean absolute error 0.14 log2 units; 58 of 88 truth-DE genes recovered, 2 genes called that the key marks as not changed (`week5_truth_key_check.txt`) |
| 10 | Figures opened and inspected (label overlap, thresholds, colour legend) | PCA: PC1 separates condition, PC2 tracks batch; volcano: dashed thresholds and counts annotated |

## 4. AI errors and revisions (documented as required)

| # | Problem | Fix |
|---|---|---|
| 1 | The starter imports `tidyverse`, which is not installed on this machine (its components are). | Loaded `ggplot2`/`dplyr`/`tibble`/`ggrepel`/`readr` directly. No analysis change. |
| 2 | The starter relies on `rstudioapi::getActiveDocumentContext()` to set the working directory; that returns an empty path under `Rscript`. | Replaced by `commandArgs("--file=")` with a `sys.frames()`/`getwd()` fallback, so the script runs identically in RStudio and from the shell. |
| 3 | First run stopped at `vst(dds, blind = FALSE)`: "less than 'nsub' rows" (default `nsub = 1000` > 989 filtered genes). | Used `vst(dds, blind = FALSE, nsub = nrow(dds))`, the documented fallback for small matrices; VST is retained. |
| 4 | Second run stopped because `plotPCA(returnData = TRUE)` returns PC1-PC2 only, so the diagnostic loop over PC3 hit `NULL`. | Loop restricted to PC1-PC2 (the variance explained is reported for both). |
| 5 | A supplementary line in the truth-key check was written as three separate vector elements, printing a broken sentence. | Wrapped in `paste0()` and regenerated the file. |
| 6 | The starter hard-codes `target_coef <- "condition_treated_vs_control"` **and** asserts it against `resultsNames(dds)`. | Kept the assertion (it is the required check) but the actual name is printed first, so the assumption is visible in the transcript. |
| 7 | Extra check: does the effect-size filter behave differently with shrunken vs unshrunken log2FC? | Yes - 60 significant with apeglm vs 74 unshrunken. The assignment requires apeglm, so the conservative number (60) is reported and the difference is recorded in `week5_truth_key_check.txt`. |

## 5. What was NOT generated by AI

The biological interpretation in `week5_interpretation.md` states only quantities produced by the
run above; the instructor key was used solely as an external check and is never read by the model
code. The provided input files were not modified (timestamps unchanged).
